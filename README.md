# Defender Vulnerability Agent

A command-line tool for assessing and enriching Microsoft Defender for Cloud vulnerabilities. This agent helps security teams analyze vulnerability data, enrich findings with contextual information, and generate comprehensive assessment reports.

To get started, install the package in development mode with `pip install -e ".[dev]"`, run tests with `pytest`, and explore available commands with `python -m dva --help`.
